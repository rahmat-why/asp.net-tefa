@model IEnumerable<ASP.NET_TEFA.Models.MsUser>

@{
    ViewData["Title"] = "Head Mechanic Account";
    Layout = "_LayoutUser";
}

<p>
    <a asp-action="Create">Create</a>
</p>
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
<table class="table">
    <thead>
        <tr>
            <th>
                Head Mechanic Name
            </th>
            <th>
                NIM
            </th>
            <th>
                Username
            </th>
            <th>
                Position
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nim)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdUser">Update</a> |
                <a asp-action="Delete" asp-route-id="@item.IdUser">Disable</a>
            </td>
        </tr>
}
    </tbody>
</table>
